services:
  backend:
    build:
      context: .
    container_name: pakecon_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PINECONE_INDEX_NAME=${PINECONE_INDEX_NAME:-pak-econ-rag}
    restart: unless-stopped