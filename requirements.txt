# ============================================================
# EC2 DEPLOYMENT NOTE:
# The bootstrap scripts (wiki_to_rag*.py, src/ingest.py) are
# NOT run on the EC2 server. Dependencies used ONLY by those
# scripts are commented out below to save disk space.
# Uncomment them locally when you need to re-generate chunks
# or re-ingest data into Pinecone.
# ============================================================

# --- Data collection (bootstrap only — NOT needed on EC2) ---
# requests
# pandas
# lxml
# html5lib
# numpy
# tabulate
# unstructured

# --- Embedding ---
# commenting out just for testing
# sentence-transformers 

# --- Vector store ---
pinecone>=3.0.0

# --- LLM + Agent ---
groq
langchain>=0.2.0
langchain-groq
langchain-community
langchain-core

# --- Utilities ---
python-dotenv
# tqdm  # bootstrap/ingest only — NOT needed on EC2

# --- Web API + UI ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
